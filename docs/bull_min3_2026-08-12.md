# BULL 最小三步（2026-08-12）

针对牛市段「追涨入场 + 快止损」负期望，冻结落地三步；**不做**回踩两档建仓、分级止盈表、牛市子状态拆分。

## 冻结值

| 项 | 值 | 落点 |
|---|---|---|
| 个股 5 日乖离硬上限 | `bias5 ≤ 3%` | `stock_selector.DEFAULT_BULL_MAX_BIAS5` |
| BULL 止损锚 | MA20、垫 2%、收盘确认 | `ma_env_exit.plan_for_env("bull_trend")` |
| BULL reason 出场标签 | `env=bull_trend\|exit=ma_env` | `_bull_sort_and_select` |
| 单票上限 | `10%` | `ASTOCK_SINGLE_CAP` 默认 / selector / pipeline |
| BULL 最多持仓 | `6` | `DEFAULT_BULL_MAX_POSITIONS`（pipeline BULL 路径） |

## 验收指标（优先于总收益）

1. BULL 入场后 **+5 日胜率 / 均值**（旧样本约 15.8% / -5.25%）
2. 出场中止损占比、平均持仓天数（观察项）
3. 日志可见 `exit=ma_env|stop_ma20_close`，`max_w≈0.10`

未过线：**禁止**拧止盈表或放宽 m4 突破门槛凑持仓数。

## 已知限制

m4 仍要求突破/锁仓，实际持仓数可能仍远低于 6；本轮只放开上限与单票权重。

## 离线对照（旧 `forward_returns.csv` BULL 19 笔回溯）

仅套用 `bias5≤3%`（不重跑选股/撮合）：

| 子集 | n | +5日胜率 | +5日均值 |
|---|---:|---:|---:|
| 全样本 | 19 | 15.8% | -5.25% |
| 会通过过滤 | 4 | 25.0% | -3.00% |
| 会被拒绝 | 15 | 13.3% | -5.85% |

方向正确但仍未转正；完整验收需新跑前向/短窗回测（`exit=ma_env|stop_ma20` + 10% 仓位一并生效）。
